/**
 * app.js — Punto di ingresso: inizializzazione al caricamento della pagina
 */

document.addEventListener('DOMContentLoaded', () => {
  renderSaved();
  renderBuiltinCats();
  renderBuiltin();
  renderTimers();
});
